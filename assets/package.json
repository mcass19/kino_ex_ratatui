{
  "name": "kino_ex_ratatui_assets",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "JS bundle for the kino_ex_ratatui Livebook widget (xterm.js + FitAddon + ImageAddon).",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "test": "node --test"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/addon-image": "^0.9.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
